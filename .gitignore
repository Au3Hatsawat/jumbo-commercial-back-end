# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment Variables (สำคัญมาก ห้ามเอาขึ้น git)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build Output (โฟลเดอร์ที่ได้จากการ build tsc)
dist/
build/
coverage/

# Prisma
# ถ้าใช้ sqlite (เผื่อไว้)
*.sqlite
*.sqlite-journal

# Generated Prisma Client (ตามที่คุณ config ไว้ใน schema.prisma)
# เราไม่ควรเอาโค้ดที่ auto-generate ขึ้น git เพราะมันสร้างใหม่ได้ด้วยคำสั่ง prisma generate
src/generated/

# Excel Files (จาก exceljs)
# กันเผลอเอาไฟล์ report ที่เทสสร้างไว้ขึ้น git
*.xlsx
*.csv
*.xls

# IDE & System Files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.Spotlight-V100
.Trashes
ehthumbs.db

# Optional: ถ้ามีไฟล์ upload
uploads/
tmp/